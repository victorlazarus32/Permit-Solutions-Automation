# Secrets — never commit
.env
*.env
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Generated data
data/*.db
data/*.txt
data/.morning_run.lock
data/leads_for_review_*
audit/*/
audit/local_*
audit/lob_template.html
inbox/*/*.xls
inbox/*/*.xlsx
inbox/processed/*/*.xls
inbox/processed/*/*.xlsx

# Tools / local binaries — don't commit
cloudflared*.exe
cloudflared-windows-*

# Playwright-installed browser binaries (downloaded at build time)
.playwright/

# Logs
logs/
*.log

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Keep folder structure markers
!.gitkeep

# Local user accounts and session secret (never commit)
data/users.json
data/.app_secret
